// operator precedence : the order in which operators are evaluated in an expression is called operator precedence.
// operator precedence is determined by the order of operations in the BODMAS rule.
// the precedence is : * / % ** + -
let result = 2 + 3 * 4;
console.log(result);

let score = ((2 + 24) - (2 * 3*(8 / 2))) * 4;
console.log(score);